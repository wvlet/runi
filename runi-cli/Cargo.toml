[package]
name = "runi-cli"
version = "0.1.0"
edition = "2024"
description = "Terminal styling and CLI utilities for the Runi library collection"
license = "Apache-2.0"

[dependencies]
nu-ansi-term = "0.50"
